/**
 * EmailJS Configuration for Portfolio Visit Tracking
 * 
 * This configuration uses the "Contact Us" template which expects:
 * - {{name}} - Visitor identifier
 * - {{time}} - Visit timestamp
 * - {{message}} - Full visit details
 * - {{email}} - Recipient email
 * 
 * All credentials are configured and ready to use!
 */

export const emailjsConfig = {
  // Your Gmail service ID (already configured)
  serviceId: 'service_iyx2t0p',
  
  // Contact Us template ID
  templateId: 'template_kayv5tl',
  
  // Public Key from EmailJS Account → General (refreshed)
  publicKey: 'kdsotuSMr22KtJxzk',
  
  // Email to receive notifications
  recipientEmail: 'gummalaprashanth509@gmail.com',
};

